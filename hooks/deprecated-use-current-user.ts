// import { useSession } from "next-auth/react";
// import { CustomUser } from "@/deprecated-auth";

// export const useCurrentUser = () => {
//   const session = useSession();
//   if (session.data?.user) return session.data.user as CustomUser;
//   return undefined;
// }
